name: benchmark
description: Ignis benchmarks, as their own runnable Linux app.
publish_to: none
version: 0.0.1
environment:
  sdk: ^3.12.0
dependencies:
  flutter:
    sdk: flutter
  benchmark_harness: ^2.3.1
  ignis:
    path: ..
  vm_service: ^15.2.0
dev_dependencies:
  flutter_test:
    sdk: flutter
  flame: ^1.38.0
  flutter_lints: ^6.0.0
